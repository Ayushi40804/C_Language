//Write a C program to extract a substring from a given string. Prompt the user to enter the starting position and length of the substring.
#include <stdio.h>
int main()
{
    int x,y,i;
    char str1[200],str2[200];
    printf("Ent er the string: ");
    gets(str1);
    printf("Enter the first end of the string: ");
    scanf("%d",&x);
    printf("Enter the second end of the string: ");
    scanf("%d",&y);
    for (i=0;str1[i]!=0;i++){
        
    }
    return 0;
}